# Serving API (FastAPI)

A tiny query surface over Postgres to demonstrate “real-time analytics” in T0.

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip3 install -r requirements.txt

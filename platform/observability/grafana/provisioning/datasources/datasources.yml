apiVersion: 1

datasources:
  - name: Prometheus
    uid: PROM
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: Postgres
    uid: PG
    type: postgres
    access: proxy
    url: postgres:5432
    user: platform
    jsonData:
      database: platform
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    secureJsonData:
      password: platform
